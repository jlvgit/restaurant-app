<h1>All <%= @category.name %> Restaurants</h1>
<br>
<% @category.restaurants.each do |restaurant| %>
 <%= link_to restaurant.name, restaurant, class:"btn btn-default" %>
<% end %>
<br><br>
<%= link_to 'Back to Categories', categories_path, class:"btn btn-primary btn-xs" %>
